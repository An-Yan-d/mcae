import trimesh
import numpy as np

import visualization as vs
 
 
PlyPath = r'F:\编程\particles_MC\mcae-main\model\Copy of Volvagia the Lava Dragon.stl\n'
print(PlyPath)
# mesh = trimesh.load(PlyPath)
# v = mesh.vertices
# f = mesh.faces

# v = np.array(v)*10
# f = np.array(f)
# # vs.show_static(v,size=1)

# # mesh2 = trimesh.Trimesh(vertices = v, faces = f)
# mesh.show()
# print(v)
# print(f)
